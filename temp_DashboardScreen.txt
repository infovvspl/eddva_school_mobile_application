"import React, { useEffect, useState } from 'react';\nimport { ScrollView, StyleSheet, Text, View, TouchableOpacity, Image, Dimensions, ActivityIndicator } from 'react-native';\nimport { Bell, Flame, Star, MessageSquare, BookOpen, ClipboardList, FileCheck, StickyNote, Bot, Library, Play, FileText, ChevronRight, CheckCircle2, Circle, GraduationCap, Trophy, LayoutDashboard, Clock, Target } from 'lucide-react-native';\nimport { student } from '../data/school-data';\nimport { schoolApi } from '../utils/api';\n\nconst { width } = Dimensions.get('window');\n\nexport function DashboardScreen({ theme }: any) {\n  const [loading, setLoading] = useState(true);\n  \n  const [stats, setStats] = useState<any>(null);\n  const [liveClasses, setLiveClasses] = useState<any[]>([]);\n  const [studyPlan, setStudyPlan] = useState<any[]>([]);\n  const [analytics, setAnalytics] = useState<any>(null);\n  const [recordings, setRecordings] = useState<any[]>([]);\n  const [materials, setMaterials] = useState<any[]>([]);\n\n  useEffect(() => {\n    const fetchDashboardData = async () => {\n      try {\n        const [\n          statsRes,\n          liveRes,\n          planRes,\n          analyticsRes,\n          recordingsRes,\n          materialsRes\n        ] = await Promise.all([\n          schoolApi.getDashboardStats(),\n          schoolApi.getLiveClasses(),\n          schoolApi.getTodayStudyPlan(),\n          schoolApi.getMyAnalytics(),\n          schoolApi.getRecordings(),\n          schoolApi.getMaterials()\n        ]);\n        \n        setStats(statsRes);\n        setLiveClasses(liveRes || []);\n        setStudyPlan(planRes || []);\n        setAnalytics(analyticsRes);\n        setRecordings(recordingsRes || []);\n        setMaterials(materialsRes || []);\n        \n      } catch (err) {\n        console.error(err);\n      } finally {\n        setLoading(false);\n      }\n    };\n    fetchDashboardData();\n  }, []);\n  \n  const colors = {\n    bg: '#F8FAFC',\n    card: '#FFFFFF',\n    primary: '#2563EB',\n    text: '#1E293B',\n  
<truncated 23302 bytes>