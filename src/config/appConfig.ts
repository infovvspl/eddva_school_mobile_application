/** When true, all services return dummy data — no network calls. */
export const USE_MOCK = false;

export const MOCK_DELAY_MS = 400;

/** Shown on login so you can confirm the latest APK is installed. */
export const APP_BUILD_LABEL = '7.89 · intro features row';

/** Student web app — used as WebView fallback for live classes. */
export const WEB_APP_BASE = 'https://dev-api.eddva.in';

/**
 * Agora App ID fallback when /live-class/token omits appId.
 * Set this to your tenant Agora App ID if needed.
 */
export const AGORA_APP_ID = '';
